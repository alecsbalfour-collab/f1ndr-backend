def build_renderer_contract(payload: dict):
    """
    Build a renderer contract from the incoming payload.
    Replace this with your real logic.
    """
    return {
        "contract": "renderer-contract",
        "input": payload
    }
