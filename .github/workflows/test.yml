name: f1ndr Backend Tests

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest httpx locust
          pip install -e .

            - name: Run tests
        env:
          MONGO_URI: "mongodb+srv://founder:f1ndrIsnum1@f1ndrcluster1.ihlkc4s.mongodb.net/f1ndr?appName=f1ndrcluster1"
          JWT_SECRET: "dev-testing-secret-key-12345"
          JWT_ALGORITHM: HS256
          ENVIRONMENT: development
        run: |
          pytest

